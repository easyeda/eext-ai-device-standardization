## 1.7.2

### 修复

1. 修复最佳匹配器件及最佳匹配器件的封装和符号显示问题
2. 添加渲染图重试逻辑，当渲染图获取失败或超时则最大重试3次

## 1.7.1

### 新增

1. 新增支持器件，符号，封装图的缩放预览
2. 新增支持库选择，允许选择创建的自己的库匹配
3. 新增深色模式
4. 新增绑定校验检测，过滤已绑定器件，可在设置自定义规则
5. 新增表头自定义，可排序，及横向滚动条预览超出范围的列表

### 修复

1. 修复匹配逻辑，严格按照匹配依据列匹配
2. 绑定功能设置变更，与EDA器件管理器设置一致
3. 修复绑定逻辑，解决部分场景下匹配失败的问题

## 1.6.1

### 新增

1. 新增AI匹配功能，允许AI匹配最佳器件
2. 新增「匹配不到器件时降级匹配封装」功能：设置中可开启降级开关并选择封装列；标准匹配用封装列值搜索封装库，AI匹配让AI推断封装关键词后搜索；封装降级结果自动使用footprint绑定模式
3. 新增更多匹配结果列表展示，可以查看近10条匹配度较高的器件
4. 新增手动匹配功能，允许手动输入关键信息检索器件库，并给出器件，符号，封装预览
5. 新增手动封装库匹配功能，允许手动输入关键信息检索封装库，并给出封装预览
6. 添加绑定功能设置，允许仅绑定封装，仅绑定符号，替换数据，替换器件功能切换
7. 支持匹配器件的匹配度计数
8. 支持匹配器件的器件图，符号图，封装图预览
9. 支持根据匹配依据列作为关键词检索